# CI/CD Pipeline test for a Java app

## Prerequisite  

Create an `.env` file first with these variables:  

```sh
GHCR_USERNAME=
GHCR_TOKEN=
MYSQL_ROOT_PASSWORD=
RABBITMQ_DEFAULT_USER=
RABBITMQ_DEFAULT_PASS=
```

